import '../Styles/Switch.css'

function Switch() {
   

    return (
        <section id="switch">
            <button id='on' className='on-off' type='button'>On</button>
            <button id='off' className='on-off' type='button'>Off</button>
        </section>
    )
}

export default Switch